# NeuroSupport — Environment Variables
# Copy this file to .env and fill in your Supabase project values.
# Never commit .env to git.

# Found in: Supabase Dashboard → Project Settings → API → Project URL
VITE_SUPABASE_URL=https://xxxxxxxxxxxxxxxxxxxx.supabase.co

# Found in: Supabase Dashboard → Project Settings → API → anon / public key
VITE_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...

# Groq API key — enables LLM-powered adaptive coaching in practice sessions
# Get it from: https://console.groq.com → API Keys
# If left empty, the app falls back to rule-based coaching messages (still works)
VITE_GROQ_API_KEY=gsk_...
